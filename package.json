{
	"private": true,
	"workspaces": [
		"backend"
	],
	"scripts": {
		"prepare": "husky install",
		"lint:all": "cd backend && yarn lint:fix"
	},
	"lint-staged": {
		"**/*.+(ts)": [
			"yarn lint:all"
		]
	},
	"devDependencies": {
		"git-commit-msg-linter": "^3.2.6",
		"husky": "^6.0.0",
		"lint-staged": "^11.0.0"
	}
}
