import styled from 'styled-components';

export const Container = styled.div`
	display: flex;
	flex-flow: column;
	min-height: 100vh;
`;

export const Main = styled.main`
	flex-grow: 1;
	display: flex;
	flex-flow: column;
`;
